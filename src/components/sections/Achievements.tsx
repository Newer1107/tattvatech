import { PlaceholderSection } from "@/components/sections/PlaceholderSection";

export function Achievements() {
  return (
    <PlaceholderSection
      id="achievements"
      label="Notable Achievements"
      heading="Verified milestones will be added here once they are documented and approved."
      copy="The section is intentionally restrained at this stage so the site does not imply awards, launches, partnerships, or scale markers that have not been publicly confirmed."
      tone="white"
    />
  );
}
